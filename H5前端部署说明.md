# 算分项目 H5 前端更新说明

## 1. 前端目录

前端源码目录：

`d:\算分小程序\scoring`

H5 打包产物目录：

`d:\算分小程序\scoring\dist\build\h5`

## 2. 打包前检查

打包前先确认生产环境接口地址是否正确。

当前线上环境：

- 前端访问地址：`http://8.163.133.224:8088`
- 后端接口地址：`http://8.163.133.224:9999/api`
- 后端 Socket 地址：`http://8.163.133.224:9999`

建议检查 `scoring/.env.production`，至少包含：

```env
VITE_API_BASE_URL=http://8.163.133.224:9999/api
VITE_SOCKET_URL=http://8.163.133.224:9999
```

## 3. 本地打包命令

在前端目录执行：

```bash
npm install
npm run build:h5
```

打包完成后，上传的是：

`dist/build/h5` 目录里的全部文件

如果需要压缩包，可在项目根目录执行：

```powershell
Compress-Archive -Path "d:\算分小程序\scoring\dist\build\h5\*" -DestinationPath "d:\算分小程序\前端H5.zip" -Force
```

## 4. 服务器更新步骤

前端是静态站点，更新时不需要重启前端服务。

更新步骤：

1. 把 `前端H5.zip` 上传到宝塔前端站点目录
2. 解压
3. 用解压后的文件覆盖站点原文件
4. 浏览器强制刷新

## 5. 更新后怎么验证

更新完成后至少检查：

1. 登录页能正常打开
2. 首页样式正常
3. 创建房间接口正常
4. 房间页样式正常
5. 房间消息、互动、计分是否正常

## 6. 前端不用重启

前端是静态文件，只要覆盖成功即可。

如果页面没变化，优先排查：

1. 是否传错目录
2. 是否没有解压覆盖
3. 是否浏览器缓存没清

## 7. 和后端联动时的注意事项

如果这次只是前端样式、文案、布局修改，通常只更新前端即可。

如果这次同时改了接口、房间规则、消息广播、Socket 行为，那就要先更新后端，再更新前端。
