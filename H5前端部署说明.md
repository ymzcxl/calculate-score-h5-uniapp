# 算分项目 H5 前端部署说明

## 当前架构

- `scoring` 是唯一前端源码目录
- `backend` 只提供 API 和 Socket 服务
- 前后端完全分离部署，前端是静态站点，后端是 Node 服务

## 当前线上环境

当前项目这套线上环境可以按下面理解：

- 前端：宝塔静态站点
- 前端访问地址：`http://8.163.133.224:8088`
- 后端服务端口：`9999`
- 后端健康检查：`http://127.0.0.1:9999/health`

当前生产环境前端通常应配置为：

```env
VITE_API_BASE_URL=http://8.163.133.224:9999/api
VITE_SOCKET_URL=http://8.163.133.224:9999
```

如果后端域名或端口变化，前端打包前必须同步修改。

## 本地开发

安装依赖：

```bash
npm install
```

启动 H5：

```bash
npm run dev
```

默认会读取 [`.env.example`](file:///d:/算分小程序/scoring/.env.example) 的配置思路。

## 环境变量

可新建 `.env.development` 或 `.env.production`：

```env
VITE_API_BASE_URL=http://localhost:3000/api
VITE_SOCKET_URL=http://localhost:3000
```

说明：

- `VITE_API_BASE_URL` 指向后端 API 根地址
- `VITE_SOCKET_URL` 指向后端 Socket 服务地址

## 打包命令

```bash
npm run build:h5
```

当前 H5 构建产物目录：

`d:\算分小程序\scoring\dist\build\h5`

主要文件包括：

- `index.html`
- `assets/*`
- `static/*`

## 日常更新发布流程

以后前端每次改完，建议固定按下面流程发布：

1. 在本地确认前端环境变量正确
2. 执行：

```bash
npm run build:h5
```

3. 将 `dist/build/h5` 目录下的全部内容上传到宝塔前端站点目录
4. 覆盖旧文件
5. 清浏览器缓存或强刷页面后验证

### 重要说明

- **前端是静态文件，更新前端后通常不需要“重启前端”**
- 只要静态文件覆盖成功，浏览器访问到新文件即可
- 如果页面还是旧样式，优先排查：
  - 宝塔站点目录是不是传错了
  - 是否只上传了 zip 没有解压覆盖
  - 浏览器缓存是否未清掉

## 后端更新时要注意

虽然这份文档属于前端项目，但前端很多功能依赖后端同步，所以这里顺手记录：

- **只更新前端样式、文案、纯前端交互：通常只需要重新打包并覆盖前端静态文件**
- **如果本次改动包含接口、Socket、房间规则、撤回逻辑、消息逻辑等后端代码：后端更新后必须重启 Node 服务**

后端当前正确启动方式是：

```bash
nohup node /www/wwwroot/score-web-app/app.js > /www/wwwroot/score-web-app/app.log 2>&1 &
```

后端更新后常用检查命令：

```bash
ps -ef | grep node
tail -n 50 /www/wwwroot/score-web-app/app.log
curl http://127.0.0.1:9999/health
```

## 宝塔发布建议

### 前端发布

推荐做法：

1. 本地执行 `npm run build:h5`
2. 把 `dist/build/h5` 里的内容打包
3. 上传到宝塔前端站点目录
4. 解压并覆盖原文件

前端发布完成后，优先检查：

1. 登录页是否打开正常
2. 首页卡片样式是否正常
3. 房间页布局是否正常
4. 浏览器控制台是否有接口报错

### 后端发布

推荐做法：

1. 上传新的后端 zip
2. 解压覆盖 `/www/wwwroot/score-web-app`
3. 重启 Node 进程
4. 用 `/health` 检查服务是否恢复

## 推荐发布顺序

如果一次更新同时涉及前后端，建议顺序如下：

1. 先更新后端
2. 重启后端并确认 `/health` 正常
3. 再更新前端静态文件
4. 打开页面做回归验证

这样可以避免前端已经用了新逻辑，但后端还在跑旧代码。

## 发布后回归清单

前端更新后至少检查：

1. 登录接口正常
2. 首页样式正常，按钮居中正常
3. 创建房间正常
4. 进入房间后 Socket 实时同步正常
5. 历史记录正常拉取

如果本次带了后端改动，再额外检查：

1. `/health` 正常返回
2. 创建房间、加入房间正常
3. 房间消息正常
4. 计分正常
5. 撤回或其他新增规则正常

## 常见问题

### 1. 前端更新后页面没变化

优先检查：

- 是否真的上传了 `dist/build/h5` 的内容
- 是否上传到了正确的宝塔站点目录
- 是否只是上传 zip 但没有解压覆盖
- 浏览器是否还在用缓存

### 2. 前端能打开，但请求失败

优先检查：

- `VITE_API_BASE_URL` 是否正确
- `VITE_SOCKET_URL` 是否正确
- 后端是否已启动
- 后端端口是否仍然是 `9999`

### 3. 后端代码解压后不生效

原因通常是：

- 只覆盖了文件，但没有重启 Node 进程
- 旧进程还在跑旧入口

当前项目后端正确入口应为：

```bash
node /www/wwwroot/score-web-app/app.js
```
