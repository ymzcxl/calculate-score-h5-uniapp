# 算分项目 H5 前端部署说明

## 当前架构

- `scoring` 是唯一前端源码目录
- `backend` 只提供 API 和 Socket 服务
- 前端和后端分开部署，互相通过地址配置连接

## 本地开发

安装依赖：

```bash
npm install
```

启动 H5：

```bash
npm run dev
```

默认会读取 [`.env.example`](file:///d:/%E7%AE%97%E5%88%86%E5%B0%8F%E7%A8%8B%E5%BA%8F/scoring/.env.example) 里对应的配置思路。

## 环境变量

可新建 `.env.development` 或 `.env.production`：

```env
VITE_API_BASE_URL=http://localhost:3000/api
VITE_SOCKET_URL=http://localhost:3000
```

生产环境示例：

```env
VITE_API_BASE_URL=https://api.score.example.com/api
VITE_SOCKET_URL=https://api.score.example.com
```

说明：

- `VITE_API_BASE_URL` 指向后端 API 根地址
- `VITE_SOCKET_URL` 指向后端 Socket 服务地址

## 打包命令

```bash
npm run build:h5
```

当前 H5 构建产物目录：

`d:\算分小程序\scoring\dist\build\h5`

主要文件包括：

- `index.html`
- `assets/*`
- `static/*`

## 部署方式

1. 先执行 `npm run build:h5`
2. 将 `dist/build/h5` 目录内容上传到静态站点目录
3. 通过 Nginx、宝塔站点或其他静态服务托管该目录
4. 确保前端站点能访问后端 API 和 Socket 地址

## 宝塔思路

- 前端：建一个静态站点，目录指向 `dist/build/h5` 的上传内容
- 后端：单独作为 Node 项目运行 `backend`
- 前端域名和后端域名可以分开，也可以由 Nginx 做反向代理

## 验证方法

前端打开后应验证这几项：

1. 登录接口正常
2. 创建房间正常
3. 进入房间后 Socket 实时同步正常
4. 历史记录正常拉取

如果页面打开了但接口失败，先检查 `VITE_API_BASE_URL` 和 `VITE_SOCKET_URL` 是否配置正确。
